{
  "mmdictEntry": {
    "prefix": "mmdictEntry",
    "description": "",
    "body": [
      "`$1` : $2,"
    ]
  },
  "mmSleep": {
    "prefix": "mmSleep",
    "description": "",
    "body": [
      "function sleep(ms) {",
      "  return new Promise(resolve => setTimeout(resolve, ms));",
      "}"
    ]
  },
  "mmRetry": {
    "prefix": "mmRetry\r",
    "description": "",
    "body": [
      "\r",
      "function retry(fn, delay=5000, retries=3, err=null) {\r",
      "  if (!retries) {\r",
      "    console.log(`(Retry Function) Failed. Out of tries.`)\r",
      "    return Promise.reject(err);\r",
      "  }\r",
      "  return fn().catch(async err => {\r",
      "      console.log(`(Retry Function) Tries remaining: ${retries}`)\r",
      "      console.log('(Retry Function) Rejected. Retrying if there are tries remaining.')\r",
      "      if (delay != 0) {\r",
      "        console.log(`Sleeping for ${delay} ms...`);\r",
      "        await sleep(delay)\r",
      "      }\r",
      "      return retry(fn, delay, (retries - 1), err);\r",
      "    });\r",
      "}"
    ]
  },
  "mmPromiseFunction": {
    "prefix": "mmPromiseFunction\r",
    "description": "",
    "body": [
      "\r",
      "function $1($2) {\r",
      "  return new Promise((resolve, reject) => {\r",
      "    \r",
      "    $3\r",
      "\r",
      "  });\r",
      "}"
    ]
  },
  "mmSelectEle": {
    "prefix": "mmSelectEle",
    "description": "",
    "body": [
      "let $1 = ()=>$2"
    ]
  }
}